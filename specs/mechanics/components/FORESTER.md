Forester — model-driven, non-deterministic. Forester proposes nodes and edges from source material. Runs as an agent skill.

Does: propose nodes and edges from source material, model-driven.
Does not: read the event log; nothing lands without user approval.
