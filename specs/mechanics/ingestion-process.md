How source material becomes approved nodes of the corpus.

Owned by Forester — full pipeline (parse → extract → dedup → infer edges → draft cards →
coverage check → propose → approve → write) in
[components/FORESTER.md](./components/FORESTER.md).

TBD - handling duplicates and near duplicate nodes (Forester stage 3).

